image boot.vfat {
	vfat {
		files = {
#BOOT_FILES#
		}
	}

	size = #BOOT_SIZE#
}

image sdcard.img {
	hdimage {
	}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		image = "boot.vfat"
	}
}
